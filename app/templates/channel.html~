{% extends "base.html" %}
{% import "bootstrap/wtf.html" as wtf%}

{% block title %}Holberton Channel directory {% endblock %}

{% block page_content %}
{% for date in dates:%}
<div class="card" style="border: 1px solid #BACAC8; padding: 10px; margin: 10px;">
  <div class="card-block">
    <h4 class="card-title">{{history[date][0]["user"]}}</h4>
    <h6 class="card-subtitle mb-2 text-muted">{{date}}</h6>
    <p class="card-text">
      {{history[date][1]["text"]}}
    </p>
  </div>
</div>



{%endfor%}
{% endblock %}
