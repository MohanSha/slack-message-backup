{% extends "base.html" %}
{% import "bootstrap/wtf.html" as wtf%}

{% block title %}Holberton Channel directory {% endblock %}

{% block page_content %}
<div class="page-header">
    <h1>Channels</h1>
    <nav class="navbar navbar-light bg-faded" style="margin-bottom:0px; padding-bottom:0px;">
      <form class="form-inline">
	<input class="form-control mr-sm-2" name="search" type="text" placeholder="Search" value="{{request.form.search}}" required>
	<button class="btn btn-outline-success my-2 my-sm-0" type="submit" value="Search">Search</button>
      </form>
      </nav>
</div>

<div class="col-sm-4">

{% for item in channel%}
{% if search in item  %}
<table class="table table-hover">
  <tbody>
    <tr>
      <td style="border-top: 0px;border-bottom: 1px solid #ddd"><a href="channel/{{item}}">{{item}}</a><br></td>
    </tr>
  </tbody>
</table>
{%elif search == "none"%}
<table class="table table-hover">
  <tbody>
    <tr>
      <td><a href="channel/{{item}}">{{item}}</a><br></td>
    </tr>
  </tbody>
  </table>
    {% endif %}
{% endfor %}
</div>

{% endblock %}
